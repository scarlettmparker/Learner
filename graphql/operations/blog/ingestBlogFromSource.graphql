mutation ingestBlogFromSource($input: IngestBlogInput!) {
  blogMutations {
    ingestBlogFromSource(input: $input) {
      message
      post {
        id
        title
        content
        language
        parentId
        type {
          id
          name
        }
        createdAt
        updatedAt
      }
    }
  }
}
