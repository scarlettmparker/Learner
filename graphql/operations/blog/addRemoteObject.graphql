mutation addRemoteObject($postId: ID!, $target: String!) {
  blogMutations {
    addRemoteObject(postId: $postId, target: $target) {
      message
      post {
        id
        title
        content
        remoteObject
        updatedAt
      }
    }
  }
}
