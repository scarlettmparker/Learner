mutation BulkCreateQuestions($inputs: [QuestionInput!]!) {
  questionMutations {
    bulkCreateQuestions(inputs: $inputs) {
      ... on QuerySuccess {
        message
        id
      }
      ... on StandardError {
        message
      }
    }
  }
}
